<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--
            Vista de lista INDEPENDIENTE para stock.quant.
            Esta vista se usa exclusivamente cuando se accede desde el botón "Stock por Atributos".
            Define una base limpia sobre la cual la lógica de Python inyectará las columnas dinámicas.
        -->
        <record id="view_stock_quant_list_with_attributes" model="ir.ui.view">
            <field name="name">stock.quant.list.with.attributes.base</field>
            <field name="model">stock.quant</field>
            <field name="arch" type="xml">
                <list>
                    <field name="product_id" optional="hide"/>
                    <field name="lot_id"/>
                    <field name="location_id"/>
                    <field name="quantity"/>
                    <field name="product_uom_id"/>
                    <!-- Campos genéricos que se mostrarán/ocultarán y cuyas etiquetas se cambiarán dinámicamente -->
                    <field name="x_attr_1" optional="show"/>
                    <field name="x_attr_2" optional="show"/>
                    <field name="x_attr_3" optional="show"/>
                    <field name="x_attr_4" optional="show"/>
                    <field name="x_attr_5" optional="show"/>
                    <field name="x_attr_6" optional="show"/>
                </list>
            </field>
        </record>
    </data>
</odoo>
