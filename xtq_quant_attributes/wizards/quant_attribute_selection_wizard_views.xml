<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_quant_attribute_selection_wizard_form" model="ir.ui.view">
            <field name="name">quant.attribute.selection.wizard.form</field>
            <field name="model">quant.attribute.selection.wizard</field>
            <field name="arch" type="xml">
                <form string="Selección de Lotes por Atributos">
                    <sheet>
                        <field name="line_ids" readonly="1">
                            <list>
                                <field name="lot_id"/>
                                <field name="location_id"/>
                                <field name="available_quantity"/>
                                <field name="quantity_to_reserve"/>
                            </list>
                        </field>
                    </sheet>
                    <footer>
                        <button string="Confirmar" name="action_confirm_selection" type="object" class="btn-primary"/>
                        <button string="Cerrar" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
