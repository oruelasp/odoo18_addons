<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.xtq.mrp.cut.liquidation</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="mrp.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='process_operations_setting_container']" position="after">
                <block title="Liquidación de Corte" name="mrp_cut_liquidation_settings">
                    <setting string="Categorías para Liquidación" help="Seleccione las categorías de productos que serán elegibles para el proceso de 'Liquidación de Corte' en las Órdenes de Producción.">
                        <field name="liquidation_product_categ_ids" widget="many2many_tags"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>

</odoo>
